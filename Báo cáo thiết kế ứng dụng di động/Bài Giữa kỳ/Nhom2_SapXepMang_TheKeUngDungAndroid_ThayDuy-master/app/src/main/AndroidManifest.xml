<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sapxepdayso_thietkeungdung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SapXepDaySo_ThietKeUngDung">
        <activity android:name=".KetQua" android:label="Kết quả sau khi sắp xếp"></activity>
        <activity android:name=".GiaiThuat" android:label="Chọn giải thuận" />
        <activity android:name=".NhapDaySo" android:label="Nhập dãy só"/>
        <activity android:name=".MainActivity" android:label="Nhóm Quán lý thu chi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>